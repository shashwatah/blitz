export enum GameMode {
    None,
    Public,
    Private
}

export enum PrivateGameMode {
    None,
    Join,
    Create
}

export const enum GameSetupPage {
    ModeSelection,
    PrivateGameSetup,
    Loading
}